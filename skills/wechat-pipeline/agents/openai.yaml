interface:
  display_name: "Ark WeChat Pipeline"
  short_description: "把公众号写作重构成素材研究优先的流水线"
  default_prompt: "Use $wechat-pipeline to run a research-first WeChat content workflow: collect source material, search the latest external context, synthesize conflicting viewpoints, run a roundtable, form the judgment and skeleton, then draft and polish for publishing."
